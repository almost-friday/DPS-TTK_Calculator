// Calculation
function timeToKill(damage, fireRate, magazineSize, reloadSpeed, health) {

}

// Updates calculation
function update() {
    const element = document.getElementById
    element("damageEntry").val
    if (element("damageEntry").value && element("fireRateEntry").value && element("magazineSizeEntry").value && element("reloadSpeedEntry").value && element("healthEntry").value) {
        console.log("Updated!")
        // timeToKill(element("damageEntry", "fireRateEntry", "magazineSizeEntry", "reloadSpeedEntry", "healthEntry"))
    }
}